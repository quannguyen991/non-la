node_modules/
*.log

# sổ ghi tiến độ và bản nháp của quy trình subagent — là giấy nháp, không phải lịch sử
.superpowers/
.DS_Store
Thumbs.db

# khoá và cấu hình riêng — anon key của Supabase là công khai, nhưng
# service_role key thì không bao giờ được vào repo
.env
.env.*
